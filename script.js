/* =========================================================
   에어사인 프로 — 랜딩페이지 스크립트 (script.js)
   - 내비게이션 앵커 부드러운 스크롤은 CSS(scroll-behavior)로 처리됩니다.
   - FAQ는 Q·A가 항상 펼쳐진 형태이므로 별도 동작이 필요 없습니다.
   - 추후 인터랙션이 필요하면 이 파일에 추가하세요.
   ========================================================= */
(function () {
  "use strict";
  // 현재는 별도 동작 없음 (정적 페이지)
})();
